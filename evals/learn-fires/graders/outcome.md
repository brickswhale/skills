---
type: llm
---
PASS if the lesson is placed by the ladder and the placement is argued. It must count the class with a command and report the number rather than a judgement — four `fromisoformat` sites in `feed/parse.py`, of which `parse_fetched` at line 17 is still unfixed, which is the proof that patching a site at a time left a sibling alive. It must take the highest rung that could actually catch a missed sibling, with the rungs above the chosen one named and ruled out for a reason; both a test suite and an installed `.githooks/` directory are available here, so landing on a briefing line alone has to be justified against them. It must name the line it replaces — `AGENTS.md:8`, "Be thorough when fixing bugs." — rather than appending a new rule beside it. The rule itself must read as an instruction for the moment it applies, not as an account of what happened in the last three weeks.
FAIL if it adds a rule without naming the line replaced, says "several" or "a few" instead of a counted number, misses that a fourth site is still broken, writes the incident story as the rule, chooses the weakest rung without ruling out the stronger ones, or proposes rules for more than the one class.
