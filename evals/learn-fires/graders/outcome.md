---
type: llm
---
PASS if the lesson is placed by the ladder and the placement is argued. It must count the class with a command and report the number rather than a judgement — four `fromisoformat` sites in `feed/parse.py`, of which `parse_fetched` at line 17 is still unfixed, which is the proof that patching a site at a time left a sibling alive.

It must check what already exists before proposing anything new, and report what it finds: `.githooks/pre-commit` is present but is an empty `exit 0` and `git config core.hooksPath` is unset, so the hook never runs; the test suite exists and nobody ran it. Those are controls that exist and do not load. The rule the skill now carries is that an unread control is a loading failure, not a missing one — so the answer must fix the loading, or raise the rule to a rung that cannot be skipped, rather than writing a second rule beside the dead one.

It must take the highest rung that could actually catch a missed sibling, with the rungs above the chosen one named and ruled out for a reason. It must name the line it replaces — `AGENTS.md:8`, "Be thorough when fixing bugs." — rather than appending a new rule beside it. The rule itself must read as an instruction for the moment it applies, not as an account of the last three weeks.

FAIL if it adds a rule without naming the line replaced; says "several" or "a few" instead of a counted number; misses that a fourth site is still broken; treats the dead hook or the unrun test suite as absent rather than as unloaded, or proposes a new control without saying why the existing one did not fire; writes the incident story as the rule; chooses the weakest rung without ruling out the stronger ones; or proposes rules for more than the one class.
