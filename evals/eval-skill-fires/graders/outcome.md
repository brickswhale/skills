---
type: llm
---
PASS if it refuses to touch the description and says why, on evidence read from the run. It must open `runs/last.jsonl` and report that the skill under test was never loaded — the init event carries a `plugin_errors` entry and its `skills` list does not contain `greet` — rather than inferring a trigger problem from the absent `Skill` call. It must call that run VOID, void, discarded or not gradeable, in those terms, and say explicitly that it is not a failing run and not evidence against the description. It must name the cause in the manifest: `.claude-plugin/plugin.json` declares an `agents` key over a directory holding nothing but `.gitkeep`, the manifest is rejected, and a rejected manifest loads zero skills. It must say the fix is to repair the load and run again.
FAIL if it rewrites greet's `description`, proposes rewriting it, or advises tuning trigger phrases; if it reports the run as red, failed or not-fired without distinguishing that from void; if it calls the run green or the skill working; if it blames the eval case, the prompt or the graders; if it never opens `runs/last.jsonl` and answers from the question alone; or if it misses that the manifest is what stopped the skill loading.
