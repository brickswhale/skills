---
type: llm
---
PASS if it opens by saying nothing will be changed, changes nothing, and argues against the idea on evidence read from the repo. It must find the reason the cache is unsafe here rather than in general: `app/perms.py` has a `revoke` that support calls when someone leaves or is compromised, so a process-local cache keeps authorizing revoked access, and `gunicorn.conf.py` sets four workers, so each holds its own copy and a restart is the only invalidation. It must name the assumptions the idea rests on and what breaks when each is wrong, and name the strongest objection outright. It must end in a judgement it is willing to make — including that this should not be built as proposed — and say what evidence would change that judgement. Naming the user's words in the field's real terms is expected where it clarifies.
FAIL if it opens with praise or agreement, or validates the idea without a substantive objection; if it produces an ordered implementation plan, file-by-file steps, or code to apply, which is a different skill's job; if it writes or edits any file; if it emits a score, rating out of ten, or any aggregate number standing in for the trade-off; if it stages named personas or a panel and presents their agreement as corroboration; if it treats the objection as a general caching caveat without finding `revoke` or the four workers; or if it refuses to reach a judgement at all.
