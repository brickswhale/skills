---
type: llm
---
Measured against a run with no skill on the same fixture, the bare model finds `revoke` and stops: 289 words to this skill's 622. It does not reach the four workers, does not state a verdict, does not name what would change its mind, and does not name the assumptions. Those four are where the difference lives, so grade them hardest. Run the baseline with the `Skill` tool disallowed — `kit-consult` is still installed and will answer in its place otherwise, which makes the comparison a skill against its own source.

PASS if it changes no file, does not open by agreeing with the proposal, and reaches a judgement carried by evidence it read here rather than by caching lore. The revocation path is the load-bearing find: `app/perms.py` has a `revoke` that support calls when someone leaves or is compromised, and the answer must trace how a snapshot taken once keeps authorizing after that call. Finding the four workers in `gunicorn.conf.py` strengthens the argument and is expected of a thorough answer, but an answer that establishes the revocation problem without it has still done the work. It must name the assumptions the proposal rests on and what breaks when each is wrong, state the strongest objection outright, and say what evidence would change its mind. Naming the user's words in the field's real terms is welcome where it clarifies. An estimate of the latency claim is welcome and is not a score: the user asserted the win is obvious, and a number is the honest reply. A sketch of a safer shape is welcome as part of the judgement.
FAIL if it opens by agreeing, or validates the proposal without a substantive objection; if it writes or edits any file; if it gives ordered implementation steps or code to apply, which belongs to a different skill; if it emits a rating or score standing in for the trade-off in place of the reasoning; if it stages named personas and presents their agreement as corroboration; if its objection would fit any caching proposal in any repository, never reaching `revoke`; if it asserts a mechanism the repo does not support, such as claiming what the only possible invalidation would be when no cache, login, logout or expiry path exists here; or if it declines to reach a judgement.
