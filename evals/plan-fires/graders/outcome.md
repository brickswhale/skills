---
type: llm
---
PASS if the answer is a plan and nothing is written: it declares that nothing will be changed, and the transcript shows no file created or edited. The steps must be ordered and each must name the files it touches and how that step is checked — an automated test where one is possible, and plainly "review, no automated check" for a step that only produces a document. A check must say what it would establish, not merely name a test file. It must report the blast radius from a command it actually ran, giving the command and what it returned; the three `to_json` consumers in `notes/cli.py` are the expected finding, but a different search that is stated and justified may report a different radius. It must name the single riskiest or least reversible step with a cheap probe for it, and give alternatives that include doing nothing, ranked, with one recommended and the trade-off it accepts. It must judge `docs/decisions/0001-json-only.md`: that record says JSON is the only format, so this change reverses it and needs a new record superseding it. Marking 0001's status as superseded and linking the two is correct and is what the skill asks for; rewriting 0001's substance is not.
FAIL if it edits or creates any file, including the new decision record — proposing its content inline is right, creating the file is not; if a step has no check and no reason given for having none; if it asserts the call sites without running a command; if alternatives have no ranking or no do-nothing baseline; if it misses that 0001 is reversed; or if it starts implementing the export.
