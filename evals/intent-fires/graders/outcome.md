---
type: llm
---
PASS if all five fields are present — problem, outcome, affected, constraints, open questions — and the repo was consulted for what the repo can settle. Two answers are on disk and must be read rather than asked: `AGENTS.md` states the list endpoint's p95 budget of 300ms, and `docs/decisions/0001-no-cache.md` bans a caching layer. Both belong under constraints, cited. `api/web.py` calling `list_items` from two entry points belongs under affected. Questions put to the user must be ones the repo cannot settle. The outcome must be phrased so a test could check it AND must not quietly narrow the complaint: the user said the page feels slow and said "lately", so an outcome that only restates the standing budget is incomplete unless it also carries the regression — or records that it is unknown. A field with no answer reads as unknown with what would settle it; any wording that conveys both the gap and its settler qualifies, the literal word is not required.
FAIL if it asks the user for the latency budget or whether caching is allowed when both are on disk; if it proposes a fix, specifies one, or starts the work; if it edits any file; if it fills a field with an invented number; or if any of the five fields is missing. Noting an observed likely cause is not a fix: the N+1 loop in `api/list.py` may be recorded as an observation, and using it to sharpen a question — whether the data grew — is good work, provided no remedy is specified.
